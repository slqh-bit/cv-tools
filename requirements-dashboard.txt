# Web dashboard only (cv_tools/dashboard.py). The CLI, library and Tkinter GUI need
# nothing here - install these on top of requirements.txt to run the dashboard:
#
#   pip install -r requirements.txt -r requirements-dashboard.txt
#   streamlit run cv_tools/dashboard.py

streamlit>=1.50

# Tap-to-pick coordinates. Optional: without it the dashboard still runs and
# falls back to the coordinate grid overlay. Version 0.4.0 imports a type alias
# that Streamlit has since removed, which cv_tools/dashboard.py shims on import.
streamlit-image-coordinates>=0.4.0
